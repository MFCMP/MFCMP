DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',  # or 'django.db.backends.postgresql', 'django.db.backends.mysql', etc.
        'NAME': BASE_DIR / 'db.sqlite3',  # or the name of your database
    }
}